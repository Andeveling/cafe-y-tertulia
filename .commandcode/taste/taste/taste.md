# Taste
- Communicates in Spanish; expects responses in Spanish. Confidence: 0.99
- Wants all user-facing content in Spanish — UI, tickets, and assistant questions — while keeping code/technical terms in English (explicitly stated: "UI en español, todo menos el código"). Confidence: 0.98
- Prefers to handle CLI authentication/secrets themselves (e.g., running `supabase login`) rather than sharing tokens or passwords in chat, even when the assistant offers to accept credentials directly. Confidence: 0.75
- Prefers the assistant to inspect the project, report what's missing/blocking, and explicitly list what inputs are needed (e.g., credentials, confirmations) before completing a task, rather than proceeding on assumptions. Confidence: 0.8
- When a framework/library changes or breaks (e.g., Next.js renaming middleware), expects the assistant to read the official docs first — including the local docs bundled in `node_modules` — before migrating code. Confidence: 0.8
- Uses Biome as the project linter (not ESLint); expects the assistant to run it and fix every reported issue — including tooling config problems such as enabling `tailwindDirectives` in `biome.json` for Tailwind v4 CSS — until lint passes clean, rather than just reporting the problems. Confidence: 0.85
- Prefers installing UI components via the shadcn CLI (e.g., `pnpm dlx shadcn@latest add ...`) rather than hand-writing them, keeping generated components as the source of truth. Confidence: 0.7
- Expects the assistant to follow the project's activated skill guidelines (e.g., shadcn skill requiring verification of imports/composition/icons after adding components) rather than just completing the task ad-hoc. Confidence: 0.6
- Expects the assistant to execute tasks end-to-end without stopping to ask for confirmation at each step once told to proceed (e.g., "adelante", "continua", "dale continua") — completing setup, verification, and cleanup autonomously, including writes to GitHub. Confidence: 0.95
- Expects completed work to be committed as part of the flow (e.g., "has el commit"), leaving a clean git tree afterward, rather than leaving changes uncommitted. Confidence: 0.7
- Prefers sonner for toast notifications in the app (explicitly requested: "espero que sea sonner"). Confidence: 0.6
- When asked where issues/tickets should live, chose GitHub Issues (accessed via the `gh` CLI) with the standard triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) rather than a local/custom tracker. Confidence: 0.75
- Delegates open design/decision questions to the assistant: when presented with several questions each carrying a recommendation (e.g., wayfinder grilling round), responds tersely "tus recomendaciones" to accept all of them and proceed, rather than answering each question individually. Confidence: 1.0
- Works within the Matt Pocock skills-based workflow and expects the assistant to follow its conventions: `/wayfinder` charts a map and hands off (never builds), one decision ticket per session working the unblocked frontier, grilling as HITL rounds of questions with recommendations, then `/to-spec` → `/to-tickets` → `/implement` (each with `/tdd` and `/code-review`), with `/compact` at phase boundaries. When unsure of the next step, the user consults the methodology (`/ask-matt`) rather than improvising. Confidence: 0.85
- When a decision ticket is resolved by grilling, the user expects the assistant to close the loop end-to-end: claim the ticket (assign to self), write the canonical glossary to `CONTEXT.md` at the repo root following the skill's format (no implementation details), post a structured resolution comment on the ticket (canonical terms with their "avoid" synonyms, agreed model decisions), close the ticket, and update the map issue's "Decisiones hasta ahora" and fog sections. Confidence: 0.95
- During wayfinder grilling rounds, the assistant is expected to present questions grouped in one message, each with a concrete recommendation and explicit "avoid" terms, letting the user accept or push back — not open-ended questions without guidance. Confidence: 0.9
- Preserves concurrent changes to shared project docs (e.g., the `CONTEXT.md` glossary): when a file changed on disk since it was last read — including entries added by other sessions/tickets — re-reads it before editing and keeps the existing content, appending only the new terms rather than overwriting. Confidence: 0.8
- When a document referenced by tickets (e.g., the PRD cited as §14/§24) isn't found in the repo, the user expects the assistant to check the GitHub map issue and issue bodies thoroughly first — reacting to a "where does the PRD live?" question with "¿no quedó en el MAP?" — rather than asking the user to locate it prematurely. Confidence: 0.6
- When asked multiple-choice questions about where a referenced document lives, the user prefers to paste the full document content directly into chat (e.g., pasting the complete PRD) over picking an option or pointing to a location/link. Confidence: 0.65
- When the user asks about the methodology or the natural next step in a workflow (e.g., "¿cuál es el paso natural según ask matt?"), expects the answer to be grounded in the project's own skill files (grep/read the actual `SKILL.md`) rather than generic knowledge or assumptions. Confidence: 0.65
- Issues terse, shorthand task directives that name the workflow step and its target (e.g., "triage al 10") instead of spelling out the steps, expecting the assistant to know the flow from the repo's skills and execute it. Confidence: 0.8
- Expects ticket triage to follow the repo's triage skill before a ticket enters the frontier: gather full context (issue body and comments, redundancy check in code, prior rejections in `.out-of-scope`, blockers) and then apply the standard labels/status, rather than triaging ad-hoc. Confidence: 0.65
 Confidence: 0.65
