# Taste
- Communicates in Spanish; expects responses in Spanish. Confidence: 0.95
- Wants all user-facing content in Spanish — UI, tickets, and assistant questions — while keeping code/technical terms in English (explicitly stated: "UI en español, todo menos el código"). Confidence: 0.95
- Prefers to handle CLI authentication/secrets themselves (e.g., running `supabase login`) rather than sharing tokens or passwords in chat, even when the assistant offers to accept credentials directly. Confidence: 0.75
- Prefers the assistant to inspect the project, report what's missing/blocking, and explicitly list what inputs are needed (e.g., credentials, confirmations) before completing a task, rather than proceeding on assumptions. Confidence: 0.8
- When a framework/library changes or breaks (e.g., Next.js renaming middleware), expects the assistant to read the official docs first — including the local docs bundled in `node_modules` — before migrating code. Confidence: 0.8
- Uses Biome as the project linter (not ESLint); expects the assistant to run it and fix every reported issue — including tooling config problems such as enabling `tailwindDirectives` in `biome.json` for Tailwind v4 CSS — until lint passes clean, rather than just reporting the problems. Confidence: 0.85
- Prefers installing UI components via the shadcn CLI (e.g., `pnpm dlx shadcn@latest add ...`) rather than hand-writing them, keeping generated components as the source of truth. Confidence: 0.7
- Expects the assistant to follow the project's activated skill guidelines (e.g., shadcn skill requiring verification of imports/composition/icons after adding components) rather than just completing the task ad-hoc. Confidence: 0.6
- Expects the assistant to execute tasks end-to-end without stopping to ask for confirmation at each step once told to proceed (e.g., "adelante", "continua") — completing setup, verification, and cleanup autonomously. Confidence: 0.7
- Expects completed work to be committed as part of the flow (e.g., "has el commit"), leaving a clean git tree afterward, rather than leaving changes uncommitted. Confidence: 0.7
- Prefers sonner for toast notifications in the app (explicitly requested: "espero que sea sonner"). Confidence: 0.6
- When asked where issues/tickets should live, chose GitHub Issues (accessed via the `gh` CLI) with the standard triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) rather than a local/custom tracker. Confidence: 0.6
- Delegates open design/decision questions to the assistant: when presented with several questions each carrying a recommendation (e.g., wayfinder grilling round), responds tersely "tus recomendaciones" to accept all of them and proceed, rather than answering each question individually. Confidence: 0.85
- Preserves locally customized shadcn-generated components: when the shadcn CLI would overwrite a customized file (e.g., `button.tsx` with Base UI `nativeButton={false}`/`render` props used across the app), inspects the diff and declines the overwrite rather than clobbering local changes. Confidence: 0.7
- Expects the full verification suite to pass before declaring completion — TypeScript typecheck, project-wide Biome lint, production build, and tests — and any test failure to be confirmed as pre-existing (e.g., via `git stash` on a clean tree) rather than attributed to the change. Confidence: 0.7
- Before continuing work, expects the assistant to run a triage pass over the open issues (reading the triage docs and using `gh` to inspect labels like `ready-for-agent`/`needs-triage`, plus checking that blocker issues are closed) to pick the next issue to work on — preferably one that is ready and unblocks the next card in the chain — and to verify the chosen issue isn't already patched/fixed before starting implementation (e.g., checking for an existing PR or local changes). Confidence: 0.7
- Expects issue-status verification to go against the actual codebase, not just the tracker: searches code, git history, SQL migrations, and tests, then reports what is already implemented vs. what genuinely remains (e.g., "the feature is in main; what's left is validation/tests, not implementation") rather than starting to build something that exists. Confidence: 0.8
- Prefers using Supabase CLI with `--linked` for investigating production issues (e.g., `supabase db query --linked "SELECT ... FROM auth.users ..."`) rather than using the Supabase dashboard; explicitly instructs "Usa supabase cli" when requesting production diagnostics. Confidence: 0.8
- Prefers realtime/interactive events (e.g., lottery/raffle draws) to be gamified and engaging — with mini-games or interactive mechanics — rather than linear/static presentations that just display results. Confidence: 0.7
